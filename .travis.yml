sudo: false
language: scala
jdk:
- oraclejdk8
scala:
- 2.12.6
- 2.11.12
script:
- nvm install 10
- sbt ++$TRAVIS_SCALA_VERSION rootJVM/test &&
     sbt ++$TRAVIS_SCALA_VERSION rootJS/test
- '[ "${TRAVIS_PULL_REQUEST}" = "false" ] && sbt updateImpactSubmit || true'
