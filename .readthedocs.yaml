version: 2

# Build from the docs/ directory with Sphinx
sphinx:
  configuration: generated-doc/out/conf.py

# Explicitly set the version of Python and its requirements
python:
  version: 3.7
  install:
    - requirements: generated-doc/out/requirements.txt